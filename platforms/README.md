# Platforms (craft only)

One file per network. **Required** whenever drafting that platform (especially § Open a run: brief → draft → editor → rubric → pack).

Loaded as `platforms/<platform>.md` after account → profile resolve. Map:

| Platform id | File |
|-------------|------|
| `x` | `x.md` |
| `linkedin` | `linkedin.md` |
| `ig` | `ig.md` |
| `instagram` | `ig.md` (canonical; `instagram.md` is redirect stub) |
| `tiktok` | `tiktok.md` |
| `threads` | `threads.md` |
| `xiaohongshu` / `xhs` | `xiaohongshu.md` |

**Not here:** ICP, pillars, voice → `profiles/`. Pixels → EasySociable `PLATFORM_SPECS`.  
**Rule:** same Topic, different platforms → **N runs**, each **re-reads** its craft file (no paste-across).  
**Missing file:** do not invent craft; stop and tell the user.
