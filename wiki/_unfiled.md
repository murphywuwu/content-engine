# Wiki unfiled

**Only** Captures (`C-`) and Needs (`N-`) that could not be hung on a lesson yet.  
Never put Hits, products, media ids, or library parts here.

| date | kind | id | note | suggested_W |
|------|------|-----|------|-------------|

Agent: do **not** invent a `W-` to clear this list. Ask the user: hang on existing `W-…` or create a named lesson. `kind` = `capture` | `need` only.

