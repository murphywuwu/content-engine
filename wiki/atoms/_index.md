# Atoms index

Techniques per role. Global library; filter with **`profiles`** + **`platforms`** + **`type`**.

**Types:** `hook` | `reframe` | `proof` | `process` | `cta` | `other`  
**Selection:** table first → drop `dead` → prefer `working` then higher `win/n` → 0–1 per needed type. **`none` allowed.**

## Status (hit-rate, our posts)

`trial` | `working` | `dead` — same meaning as `swipe/_index.md`.  
`n` / `win` / `loss` = our published cache. Do not share claim’s epistemic words.  
**No Evidence table in atom files.** Use history = `published/_index` (column `atoms`).

## Ingest

`CLAUDE.md` § Library ingest. Capture offer path A; publish path B + gates.

## Catalog

| id | file | type | platforms | profiles | status | n | win | loss | one_liner |
|----|------|------|-----------|----------|--------|---|-----|------|-----------|

`profiles: *` = all profiles.
