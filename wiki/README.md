# wiki/

Split library (option B):

| Path | What | Run selection |
|------|------|----------------|
| `atoms/` | Techniques per role (hook, cta, proof, …) | Match swipe beats / roles; **none OK** |
| `claims/` | Falsifiable angles / learned strategies | 0–1 claim; **none OK** |

Selection always starts at each folder’s `_index.md` (Approach B).  
Filter rows by **platform** and **profiles** (`*` = all profiles) from the current account’s profile_id.  
Drop atom `dead` and claim `retired`. Prefer atom `working`. Claim `status` is epistemology; rank claims by `win/n`.  
Atom use history = `published/_index`. Claim proposition evidence = `## Evidence` in the claim file (not in atoms).
