# Claims index

Falsifiable angles. Global library; filter with **`profiles`** + **`platforms`**.

**Selection:** 0–1 claim per run. Drop `retired`. **`hypothesis` is valid to pick** (that is the test). Rank remaining by `win/n`, not by epistemic status. **`none` allowed.**

## Status (epistemology — “do we still assert this”)

`hypothesis` | `supported` | `weakened` | `retired`

This is **not** hit-rate. Proposition ledger = `## Evidence` in **this claim’s file**.

## Hits (separate)

`n` / `win` / `loss` = our `published.result` when this claim was used. Updating hits **must not** auto-change `status`.

## Ingest / promote

`CLAUDE.md` § Library ingest. No single-win → `supported`. `supported` = ≥3 runs that support the **proposition** + user 批准.

## Catalog

| id | file | type | platforms | profiles | status | n | win | loss | one_liner |
|----|------|------|-----------|----------|--------|---|-----|------|-----------|

`profiles: *` = all profiles.
